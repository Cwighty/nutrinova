import { Typography } from "@mui/material";
import { PageContainer } from "@/components/PageContainer";

export default function MyFoodsPage() {
  return (
    <PageContainer title={"My Foods"}>
      <Typography variant={"h1"}>My Foods</Typography>
    </PageContainer>
  );
}
